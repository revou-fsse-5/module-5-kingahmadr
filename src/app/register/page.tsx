import React from "react";
import MultiStepForm from "../modules/MultiStepForm";

const page = () => {
  return (
    <div className="register-page">
      <MultiStepForm />
    </div>
  );
};

export default page;
