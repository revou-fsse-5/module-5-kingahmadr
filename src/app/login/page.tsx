import React from "react";
import LoginForm from "../components/LoginForm";

const page = () => {
  return (
    <div className="login-page">
      <LoginForm />
    </div>
  );
};

export default page;
